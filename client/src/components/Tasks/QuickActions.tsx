import React from "react";
import Box from "@mui/material/Box";
import { Typography } from "@mui/material";

const QuickAction = () => {
  return (
    <Box>
      <Box>
        <Typography>Start pomodoro</Typography>
        <button>START</button>
      </Box>

      <Box>box two</Box>
    </Box>
  );
};
export default QuickAction;
